// Randomize the animation speed
image_speed = random_range(.85, 1);

// Amount of fire objects to create after destruction
fireAmount = round(random_range(2, 5));
fireMaxOffset = 5;
fireSpawned = false;
isAllied = false;
