// Notify the player that the client is attempting to connect
draw_set_halign(fa_center);
draw_text(room_width / 2, (room_height / 2) - 10, "Connecting");

// Set the draw settings back to default
draw_set_halign(fa_left);
