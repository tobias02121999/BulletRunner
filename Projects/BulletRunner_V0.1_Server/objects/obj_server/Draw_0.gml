// Notify the user that the server is running
draw_text(4, 4, "Match initialized for " + string(maxClients) + " client(s)");
draw_text(4, 28, "Server is running . . .");
draw_text(4, 52, "Client 0 Score: " + string(socketScore[0]));
draw_text(4, 76, "Client 1 Score: " + string(socketScore[1]));
